// Service disabled by user request to remove AI dependency.
// This file is kept to maintain directory structure if needed, but is effectively empty.

export const generateMotivation = async (
  status: 'TAKEN' | 'MISSED',
  streak: number,
  balance: number
): Promise<string> => {
  return "";
};
